# Backlog

Deferred scope items with promotion criteria.

Items here are not in the active generation queue. They should be promoted to
an active Northstar task only when the prerequisites and planning work that
blocks them are complete.

## Items

- [`001-macos-preview-lane-disposition.md`](001-macos-preview-lane-disposition.md)
  — decide whether the retained macOS IOSurface preview lane stays as
  operator-only diagnostics or moves onto a later removal/simplification track

## Promotion Rule

An item moves from backlog to an active task when:
- the prerequisite work it depends on is complete
- the contracts and architecture it needs exist
- a maintainer decides the timing is right

Do not promote backlog items into active tasks speculatively.
